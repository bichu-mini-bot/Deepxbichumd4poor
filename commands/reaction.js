module.exports = {
    // ========== .laugh ==========
    laugh: async (message, args, client) => {
        await message.reply('😂 *Laughs*');
    },

    // ========== .shy ==========
    shy: async (message, args, client) => {
        await message.reply('😊 *Shy*');
    },

    // ========== .sad ==========
    sad: async (message, args, client) => {
        await message.reply('😢 *Sad*');
    },

    // ========== .moon ==========
    moon: async (message, args, client) => {
        await message.reply('🌙 *Moon*');
    },

    // ========== .anger ==========
    anger: async (message, args, client) => {
        await message.reply('😡 *Angry*');
    },

    // ========== .happy ==========
    happy: async (message, args, client) => {
        await message.reply('😊 *Happy*');
    },

    // ========== .confused ==========
    confused: async (message, args, client) => {
        await message.reply('😕 *Confused*');
    },

    // ========== .heart ==========
    heart: async (message, args, client) => {
        await message.reply('❤️ *Heart*');
    },

    // ========== .cool ==========
    cool: async (message, args, client) => {
        await message.reply('😎 *Cool*');
    },

    // ========== .fire ==========
    fire: async (message, args, client) => {
        await message.reply('🔥 *Fire*');
    },

    // ========== .star ==========
    star: async (message, args, client) => {
        await message.reply('⭐ *Star*');
    },

    // ========== .thumbsup ==========
    thumbsup: async (message, args, client) => {
        await message.reply('👍 *Thumbs up*');
    }
};